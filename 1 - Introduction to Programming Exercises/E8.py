widgets = int(input('Type quantity of widgets: '))
gizmos = int(input('Type quantity of gizmos: '))

total_weight = (widgets * 75) + (gizmos * 112)

print('The total weight of the order is: %i' %(total_weight) + 'g')